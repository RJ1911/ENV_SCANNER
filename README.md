# ENV_SCANNER
```bash 
usage : node envScanner.js [directoryName] [Extra fileExtensions]
directoryName : if in current directory ""
                else use relative directory path
Extra fileExtensions : some other custom file extesnsions which might contain process.env.some_env_var type text
e.g usage : node envSanner.js "./src" mjs tsx 
```

